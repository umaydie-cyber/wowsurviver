{
  "name": "azeroth-survivor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run"
  },
  "dependencies": { "phaser": "^3.90.0" },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.7.2",
    "vite": "^6.1.0",
    "vitest": "^3.0.5"
  },
  "engines": { "node": ">=20" }
}
